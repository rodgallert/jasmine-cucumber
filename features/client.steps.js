const assert = require('assert');
const { Given, When, Then } = require('cucumber');

Given('I have {string} clients in the database', function (qty) {
    return 'pending';
});

When('I access the home page', function () {
    return 'pending';
});

Then('List {string} clients', function (qty) {
    return 'pending';
});